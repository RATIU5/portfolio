---
import Page from "../layouts/page.astro";
---


<Page>
  <canvas id="upperDrip" class="h-[400px] w-full"></canvas>
</Page>

<script>
import { createScene, createCanvas } from "../lib/canvas";

let canvasEl = document.getElementById("upperDrip") as HTMLCanvasElement;
const state = {

};
const scene = createScene<typeof state>([
  createCanvas<typeof state>(canvasEl, {
    setup: () => {

    },
    resize: (canvas) => {
      canvas.width = window.innerWidth;
      canvas.height = 400;
    },
    paint: ({ctx, state}) => {
      ctx.fillStyle = "blue";
      ctx.rect(100, 100, 100, 100);
    },
  })
]);
</script>

